import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    var exceperiance = [
      'Flutter',
      'Dart',
      'Firebase',
      'Git',
      'PHP',
      'Python',
      'Java',
    ];
    var tasks = [
      'فهم استخدامات Stack widget',
      'بناء واجهة باستخدام Row and Column Widget',
      'التدريب على Wrap Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
      'استخدام ال ListView.builder Widget',
    ];
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'User Profile',
          style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold,color: Colors.white),
        ),
        centerTitle: true,
        backgroundColor: Colors.black,
        toolbarHeight: 100,
      ),
      body: Container(
        padding: EdgeInsets.all(10),
        color: Colors.black,
        child: ListView(
          children: [
            Container(
              margin: EdgeInsets.all(10),
              child: Stack(
                clipBehavior: Clip.none,
                alignment: AlignmentDirectional.topEnd,
                children: [
                  CircleAvatar(radius: 70, child: Icon(size: 50, Icons.person)),
                  Positioned(
                    top: 110,
                    child: CircleAvatar(
                      radius: 15,
                      backgroundColor: Colors.blueGrey,
                      child: Icon(color: Colors.white, Icons.edit),
                    ),
                  ),
                ],
              ),
            ),
            Container(
              alignment: AlignmentDirectional.topStart,
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(
                    'legend',
                    style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold,color: Colors.white),
                  ),
                  Row(
                    children: [
                      Icon(Icons.email, color: Colors.white),
                      Container(
                        margin: EdgeInsets.all(10),
                        child: Text(
                          'legend@example.com',
                          style: TextStyle(color: Colors.white, fontSize: 20),
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),
            Divider(),
            Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                  'المهارات :- ',
                  style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold,color: Colors.white),
                ),
                Wrap(
                  direction: Axis.horizontal,
                  children: [
                    for (int i = 0; i < exceperiance.length; i++)
                      Container(
                        margin: EdgeInsets.all(5),
                        padding: EdgeInsets.all(10),
                        child: Text(exceperiance[i]),
                        decoration: BoxDecoration(
                          color: Colors.white,
                          shape: BoxShape.rectangle,
                          border: Border.all(width: 1),
                          borderRadius: BorderRadius.all(Radius.circular(10)),
                        ),
                      ),
                  ],
                ),
              ],
            ),
            Divider(),
            Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [

                Text(
                  'قائمة المهام :-',
                  style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold,color: Colors.white),
                ),
                for (int i = 0; i < tasks.length; i++)
                  Row(
                    children: [
                      Icon(Icons.check_circle_outline,color: Colors.white,),

                      Container(
                        margin: EdgeInsets.all(10),
                        child: Text(tasks[i],style: TextStyle(color: Colors.white),),
                      ),
                    ],
                  ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
